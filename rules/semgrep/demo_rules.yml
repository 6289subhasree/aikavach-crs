rules:
  - id: aikavach.python.subprocess-shell-true
    message: User-controlled command execution may be possible when subprocess uses shell=True.
    severity: WARNING
    languages:
      - python
    patterns:
      - pattern: subprocess.$FUNC(..., shell=True, ...)
